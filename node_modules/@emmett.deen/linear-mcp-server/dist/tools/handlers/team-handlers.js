import { logError } from "../../utils/config.js";
/**
 * Handler for getting teams
 */
export function handleGetTeams(linearService) {
    return async (args) => {
        try {
            return await linearService.getTeams();
        }
        catch (error) {
            logError("Error getting teams", error);
            throw error;
        }
    };
}
