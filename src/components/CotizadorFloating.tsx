import React from 'react';

interface CotizadorFloatingProps {
  className?: string;
  position?: 'bottom-left' | 'bottom-right' | 'top-left' | 'top-right';
}

const CotizadorFloating: React.FC<CotizadorFloatingProps> = () => {
  return null;
};

export default CotizadorFloating;